import { SelectRequiredValidatorDirectiveDirective } from './select-required-validator-directive.directive';

describe('SelectRequiredValidatorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectRequiredValidatorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
